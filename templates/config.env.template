# web-access skill 使用者偏好配置
# 此檔案是模板。首次執行 check-deps.mjs 時會自動複製為 config.env（git ignored）。
#
# 使用者長期偏好的預設 Agent 瀏覽器
# - 留空：每次啟動都問使用者偏好哪個
# - 設值：固定使用該瀏覽器；若未檢測到對應 toggle 則硬錯（不擅自降級）
#
# 合法值：chrome / edge
WEB_ACCESS_BROWSER=
